{
  "name": "crud",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "bun@1.3.13",
  "scripts": {
    "setup": "bun install && bun scripts/setup.mjs",
    "dev": "concurrently \"bun --cwd apps/frontend dev\" \"bun scripts/activate.mjs uvicorn app.main:app --reload\"",
    "start": "bun --cwd apps/frontend build && concurrently \"bun --cwd apps/frontend start\" \"bun scripts/activate.mjs uvicorn app.main:app --host 0.0.0.0 --port 8000\"",
    "lint": "bun --cwd apps/frontend lint",
    "format": "bun --cwd apps/frontend format"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "concurrently": "^10.0.3"
  }
}